{
  "dependencies": {
    "gh-pages": "^6.1.1",
    "three": "^0.161.0"
  },
  "devDependencies": {
    "vite": "^5.1.1"
  },
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "deploy": "vite build && gh-pages -d dist"
  }
  
}
